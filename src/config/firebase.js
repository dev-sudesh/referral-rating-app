// Firebase configuration
// Note: The actual configuration is handled by google-services.json (Android) 
// and GoogleService-Info.plist (iOS) files

// Optional: Add additional Firebase configuration here if needed
export const firebaseConfig = {
    // Add any additional configuration here
    // Most configuration is handled automatically by the native config files
};

// Firebase initialization is handled automatically by @react-native-firebase/app
// when the config files are properly placed in the native projects
